const LOGIN = 'LOGIN'
const SIGN_UP = 'SIGN_UP'
const LOGOUT = 'LOGOUT'

const login = () => {
    return {
        type: LOGIN,
    }
}

const signUp = () => {
    return {
        type: LOGIN,
    }
}

const signOUt = () => {
    return {
        type: LOGIN,
    }
}